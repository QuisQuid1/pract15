<Page x:Class="TRPO_pr15.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TRPO_pr15.Pages"
      xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Форма входа">
    <Grid Background="#dfeaeb" >
        <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <TextBlock Text="Вход в приложение" 
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="10"/>
            <PasswordBox x:Name="login">
                <MaterialDesign:HintAssist.Hint>
                    Введите пароль
                </MaterialDesign:HintAssist.Hint>
            </PasswordBox>

            <Button Content="Войти как менеджер" Click="ManagerButton_Click"/>
            <Button Content="Войти как посетитель" Click="UserButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
