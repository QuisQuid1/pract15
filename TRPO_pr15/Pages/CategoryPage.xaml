<Page x:Class="TRPO_pr15.Pages.CategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:validation="clr-namespace:TRPO_pr15.Validations"
      xmlns:local="clr-namespace:TRPO_pr15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Категории">

    <Grid Background="#dfeaeb">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label FontSize="24" FontWeight="Bold" HorizontalAlignment="Center">Категории</Label>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                    Background="White"
                    CornerRadius="5">
                <StackPanel>
                    <Label FontWeight="Bold" >Добавление/редактирование</Label>
                    <StackPanel Orientation="Horizontal">
                        <Label VerticalAlignment="Center">Название:</Label>
                        <TextBox x:Name="Name" VerticalAlignment="Center">
                            <TextBox.Text>
                                <Binding Path ="TextBoxCategory.Name">
                                    <Binding.ValidationRules>
                                        <validation:EmptyValidation/>
                                    <validation:MaxLengthValidation MaxLength="100"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Click="AddButton_Click" >Добавить</Button>
                        <Button Click="RemoveButton_Click">Удалить</Button>
                        <Button Click="EditButton_Click">Редактировать</Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.RowSpan="2" Grid.Column="1" 
                    Background="White"
                    CornerRadius="5"
                    Padding="10"
                    Margin="5,0,0,5">
                <StackPanel>
                    <StackPanel>
                        <Label FontWeight="Bold">Список категорий</Label>
                        <MaterialDesign:ColorZone Mode="SecondaryMid" Padding="5">
                            <ListView x:Name="ProductsList" 
                 HorizontalContentAlignment="Stretch" 
                 ItemsSource="{Binding _categoryService.Categories}" 
                 SelectedItem="{Binding SelectedCategory, UpdateSourceTrigger=PropertyChanged}"
                 SelectionChanged="ProductsList_SelectionChanged">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Padding="5"/>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </MaterialDesign:ColorZone>
                    </StackPanel>
                    <Button Grid.Row="1" Grid.Column="1"
        Click="BackButton_Click"
        HorizontalAlignment="Right">Назад</Button>

                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
