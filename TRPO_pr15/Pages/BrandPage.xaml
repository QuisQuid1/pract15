<Page x:Class="TRPO_pr15.Pages.BrandPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:TRPO_pr15.Pages"
      xmlns:validation="clr-namespace:TRPO_pr15.Validations"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Бренды">

    <Grid Background="#dfeaeb">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Добавление/редактирование брендов" 
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="10"/>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                    Background="White"
                    CornerRadius="5">
                <StackPanel>
                    <Label FontWeight="Bold">Добавление/редактирование</Label>
                    <StackPanel Orientation="Horizontal">
                        <Label VerticalAlignment="Center">Название:</Label>
                        <TextBox x:Name="Name" VerticalAlignment="Center">
                        <TextBox.Text>
                                <Binding Path ="TextBoxBrand.Name">
                                <Binding.ValidationRules>
                                    <validation:EmptyValidation/>
                                    <validation:MaxLengthValidation MaxLength="100"/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                        </TextBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Click="AddButton_Click">Добавить</Button>
                        <Button Click="RemoveButton_Click">Удалить</Button>
                        <Button Click="EditButton_Click">Редактировать</Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.RowSpan="2" Grid.Column="1"
                    Background="White"
                    CornerRadius="5">
                <StackPanel>
                    <StackPanel >
                        <Label FontWeight="Bold">Список брендов</Label>
                        <MaterialDesign:ColorZone Mode="SecondaryMid" Padding="5">
                            <ListView x:Name="ProductsList" 
                                 HorizontalContentAlignment="Stretch" 
                                 ItemsSource="{Binding _brandService.Brands}" 
                                 SelectedItem="{Binding SelectedBrand, UpdateSourceTrigger=PropertyChanged}"
                                 SelectionChanged="ProductsList_SelectionChanged">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Padding="5"/>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </MaterialDesign:ColorZone>
                    </StackPanel>
                    <Button
                    Click="BackButton_Click"
                    HorizontalAlignment="Right"
                    Width="100">Назад</Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
